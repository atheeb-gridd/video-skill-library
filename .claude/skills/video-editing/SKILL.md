---
name: video-editing
description: Reproduce the CUT of a reference video. Analyse how a reference is cut — cut types, pacing, shot-length distribution, structure, retention devices — then design the edit decision list for a new video in that style. Use when matching a reference video's editing, when asked for cuts or pacing or an EDL, or when the user names the editing skill.
---

# Editing (loader)

The real library lives in the visible vault so it is browsable, taggable and linkable in Obsidian. This file only routes you there.

**Read these now, in order:**

> **Paths below resolve from your cwd, not from this file's directory. Do not `cd`** — a
> vault-root path that looks missing usually means you changed directory, not that the file
> is absent.


1. `skills/EDITING/SKILL.md` — the routing logic and the two modes (ANALYSE / DESIGN)
2. `_meta/pipeline.md` — the five-stage contract: video + transcript → design documents → build manifest
3. `_meta/tags.md` — the closed tag vocabulary used to query the library
4. `_meta/execution-contract.md` — what this stack can actually execute

**The rule library:** `skills/EDITING/rules/` — one note per technique. Each note's *Reproduction prompt* is the executable payload; its *How to recognise it in a reference video* section is what makes the analysis pass work.

**Templates:** `_templates/design-cuts.md`, `_templates/style-profile.md`

Cuts are designed **first** — motion, sound and subtitles all attach to the timeline cuts define. Hand off in that order.
